spring:
  application:
    name: gateway

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    gateway:
      routes:
        - id: contact-service
          uri: lb://CONTACT-MS
          predicates:
            - Path=/contacts/**
        - id: user-service
          uri: lb://USER-MS
          predicates:
            - Path=/user/**
