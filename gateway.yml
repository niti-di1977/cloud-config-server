spring:
  application:
    name: api-gateway

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    gateway:
      routes:
        - id: contact-service
          uri: lb://contact-ms
          predicates:
            - Path=/contacts/**
        - id: user-service
          uri: lb://user-ms
          predicates:
            - Path=/user/**
